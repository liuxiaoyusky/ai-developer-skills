# dev-verify Skill

> **Talk is cheap. Show me the test results.** - Linus Torvalds (modified)

**dev-verify** 是 dev-flow 生态系统的自动化测试验证执行器。它填补了测试规划（dev-flow）和问题解决（dev-debug）之间的空白，通过**真实执行测试**（非 AI 模拟）来验证代码质量。

## 核心特性

1. ✅ **真实测试执行** - 通过 Bash 工具运行实际测试，不依赖 AI 模拟
2. 📊 **Linus Torvalds 5层代码审查** - 在测试前进行深度代码分析
3. 🤖 **自动框架检测** - 支持 pytest, jest, go test, JUnit 等主流框架
4. 🔧 **失败自动调试** - 测试失败时自动调用 dev-debug 技能
5. 📝 **完整结果记录** - 所有结果自动标记到 `task-details-test.md`

## 快速开始

### 自动激活（推荐）

dev-verify 会在以下场景自动激活：

1. **dev-flow 集成**：进入 dev-flow Step 4 测试验证阶段时自动调用
2. **手动请求**：说 "验证测试"、"运行测试"、"执行测试"、"dev-verify"

### 手动激活

```
用户: "验证测试"
AI: [激活 dev-verify 技能]
```

## 工作流程

```
1. 文件检查
   ↓ 检查 task-details-test.md 是否存在
   ↓ 不存在则从 tasks.md 生成

2. 代码审查
   ↓ 应用 Linus Torvalds 5层分析
   ↓ 更新 task-details-test.md

3. 框架检测
   ↓ 自动检测测试框架
   ↓ pytest/jest/go test/JUnit/etc

4. 执行测试
   ↓ 通过 Bash 工具真实执行
   ↓ 捕获输出和结果

5. 标记结果
   ↓ 更新 task-details-test.md
   ↓ 记录通过/失败详情

6. 处理失败
   ↓ 对每个失败调用 dev-debug
   ↓ 修复后重新测试
   ↓ 循环直到全部通过
```

## 支持的测试框架

### v0.1.0

| 语言 | 框架 | 检测依据 | 命令 |
|------|------|----------|------|
| Python | pytest | pytest.ini, pyproject.toml, requirements.txt | `pytest -v --tb=short` |
| JS/TS | jest | package.json: "jest" | `npm test -- --verbose` |
| JS/TS | vitest | package.json: "vitest" | `npm test -- --run` |
| JS/TS | mocha | package.json: "mocha" | `npm test` |
| Go | go test | *_test.go files, go.mod | `go test -v ./...` |
| Java | JUnit + Maven | *Test.java + pom.xml | `mvn test` |
| Java | JUnit + Gradle | *Test.java + build.gradle | `gradle test` |

### 后续版本计划

- Ruby (RSpec)
- .NET (NUnit, xUnit)
- PHP (PHPUnit)
- Rust (cargo test)

## 集成到 dev-flow

### dev-flow Step 4 修改

**原始行为**：手动测试执行

**新行为**：自动调用 dev-verify

```markdown
### Step 4: 测试验证（使用 dev-verify）

1. 自动调用 dev-verify 技能
2. dev-verify 执行：
   - Linus 5层代码审查
   - 真实测试执行
   - 结果标记
   - 失败自动调用 dev-debug
3. 等待所有测试通过
4. 更新 tasks.md（移动到 DONE）
```

## 测试结果格式

### task-details-test.md 示例

```markdown
## 📊 代码审查（Linus 5层分析）

**审查时间**: 2025-02-05 14:35
**评分**: 8/10

### Layer 1: 数据结构
- 核心数据：User, Session
- 数据流：→ Auth API → Database → Response

---

## 1. 单元测试

**状态**: ✅ PASSED (15/15)
**执行时间**: 2025-02-05 14:35:22
**框架**: pytest
**命令**: `pytest -v --tb=short`

**输出**:
```
tests/test_auth.py::test_login PASSED
tests/test_auth.py::test_register PASSED
...
========== 15 passed in 2.34s ==========
```

---

## 2. 集成/功能测试

**状态**: ❌ FAILED (5/8 passed, 3 failed)
**执行时间**: 2025-02-05 14:36:10

### 🔧 Debug Session #1

**时间**: 2025-02-05 14:36:15
**根因**: Database connection not closed
**修复**: Added finally block in api/auth.py:67
**验证**: ✅ PASSED (re-test)

---

## 📋 测试汇总

| 类别 | 状态 | 通过/总计 | 时间 |
|------|------|----------|------|
| 单元测试 | ✅ | 15/15 | 2.3s |
| 集成测试 | ✅ | 8/8 | 10.5s |
| 启动测试 | ✅ | 通过 | 5.2s |

**总体**: ✅ 所有测试通过
```

## 代码审查框架

### Linus Torvalds 5层分析

**Layer 1: 数据结构分析**
- 核心数据模型识别
- 数据流向追踪
- 潜在问题标记

**Layer 2: 特殊情况识别**
- 边界条件列举
- 错误处理检查
- 特殊情况（空值、null、超长输入）

**Layer 3: 复杂度审查**
- 嵌套深度计算（>3 需标记）
- 抽象层次评估
- 圈复杂度评估

**Layer 4: 破坏性分析**
- API 兼容性验证
- 行为变更检查
- 迁移成本评估

**Layer 5: 实用性验证**
- 真实问题确认
- 过度工程评估
- 实用性评分

## 故障排除

### 问题 1: 未检测到测试框架

**症状**：
```
⚠️  未检测到测试框架
```

**解决方案**：
1. Python: 创建 `pytest.ini` 或 `pyproject.toml`
2. JavaScript: 在 `package.json` 添加 test 脚本
3. Go: 确保 `*_test.go` 文件存在
4. Java: 创建 `pom.xml` 或 `build.gradle`

### 问题 2: 测试超时

**症状**：
```
⏱️  测试超时（超过 5 分钟）
```

**解决方案**：
1. 检查测试是否陷入死循环
2. 检查外部服务是否可用
3. 考虑跳过慢速测试
4. 增加超时时间配置

### 问题 3: 依赖缺失

**症状**：
```
❌ ModuleNotFoundError: No module named 'pytest'
```

**解决方案**：
```bash
# Python
pip install pytest

# JavaScript
npm install

# Go
go mod download

# Java
mvn dependency:resolve
```

## 日志

所有操作记录到 `dev-verify.log`：

```markdown
[2025-02-05 14:35:10] [START] dev-verify 开始
[2025-02-05 14:35:12] [REVIEW] Linus 5层代码审查
[2025-02-05 14:35:15] [DETECT] 测试框架检测 → pytest
[2025-02-05 14:35:16] [EXECUTE] 单元测试执行 → 15 passed
[2025-02-05 14:35:20] [EXECUTE] 集成测试执行 → 5 passed, 3 failed
[2025-02-05 14:35:22] [INVOKE] 调用 dev-debug
[2025-02-05 14:35:45] [FIXED] dev-debug 完成
[2025-02-05 14:36:00] [COMPLETE] dev-verify 完成
```

## 相关技能

- **[dev-flow](../dev-flow/)** - 任务管理和 5步工作流（调用 dev-verify）
- **[dev-debug](../dev-debug/)** - 5 Whys + 第一性原理调试（被 dev-verify 调用）
- **[dev-review](../dev-review/)** - Linus 5层代码审查（提供审查框架）

## 架构设计

### 核心原则

1. **真实执行 > AI 模拟** - 确保测试结果可靠
2. **自动激活从 dev-flow Step 4** - 确保测试不被跳过
3. **测试前代码审查** - 在浪费时间之前捕获结构问题
4. **多层框架检测** - 优雅降级到手动测试
5. **结果标记在源文件** - 单一数据源
6. **每个失败调用 dev-debug** - 隔离根因分析

### 文件结构

```
general/dev-verify/
├── SKILL.md                           # 主技能定义
├── README.md                          # 本文件
├── CHANGELOG.md                       # 版本历史
├── templates/
│   └── task-details-test.template.md  # 测试方案模板
└── framework-detectors/
    ├── detect-python.sh               # Python 框架检测
    ├── detect-javascript.sh           # JS/TS 框架检测
    ├── detect-go.sh                   # Go 框架检测
    └── detect-java.sh                 # Java 框架检测
```

## 贡献指南

### 添加新的测试框架支持

1. 在 `framework-detectors/` 创建检测脚本
2. 更新 `SKILL.md` 的框架支持列表
3. 添加相应的测试命令
4. 更新本 README 的支持列表

### 示例：添加 Ruby RSpec 支持

**detect-ruby.sh**:
```bash
#!/bin/bash
if [ -f "Gemfile" ] && grep -q "rspec" Gemfile; then
    echo "rspec"
elif find . -name "*_spec.rb" | grep -q .; then
    echo "rspec"
else
    echo "unknown"
fi
```

**SKILL.md 更新**:
```markdown
| Ruby | RSpec | Gemfile, *_spec.rb | `rspec` |
```

## 许可证

本技能是 ai-developer-skills 项目的一部分。

---

**End of README**
