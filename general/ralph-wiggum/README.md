# Ralph Wiggum Skill

基于第一性原理重构的极简 Ralph Wiggum - 让 AI 持续迭代直到完成所有任务。

## 什么是 Ralph Wiggum？

Ralph Wiggum 是一个 AI 开发方法论，通过简单的循环让 Claude Code 持续迭代，自主完成开发任务。

**核心思想**：
- **默认第一次不会写对** - 接受 AI 需要多次迭代
- **失败是有价值的数据** - 错误信息帮助 AI 改进
- **持续直到完成** - 自动重试，无需人工干预

## 核心特性

- ✅ **极简架构** - 仅 3 个组件：任务文件、循环脚本、AI 调用
- ✅ **跨平台支持** - Node.js 在 Linux/macOS/Windows 上均可运行
- ✅ **零配置运行** - 编辑 1 个文件，运行 1 个命令
- ✅ **自动完成检测** - 基于任务清单，自动停止
- ✅ **70% 代码减少** - 从第一性原理重构，移除所有非必要复杂性

## 快速开始

### 30 秒上手

```bash
# 1. 创建任务文件
cp TASKS.template.md TASKS.md

# 2. 编辑任务（添加你的项目目标）
vim TASKS.md

# 3. 运行
chmod +x loop.js
./loop.js
```

**就这么简单！** Ralph 会持续工作，直到所有任务完成。

---

## 工作原理

### 3 个核心组件

```
┌─────────────────────────────────────┐
│           TASKS.md                  │
│  - 项目目标                         │
│  - 任务清单（checkbox）             │
│  - 验证命令                         │
└─────────────────────────────────────┘
              ↑
              │ 读取
              │
              ↓
┌─────────────────────────────────────┐
│          loop.js (10 行)            │
│  - 检查未完成任务                   │
│  - 调用 Claude CLI                  │
│  - 重复直到全部完成                 │
└─────────────────────────────────────┘
              ↑
              │ 调用
              │
              ↓
┌─────────────────────────────────────┐
│         Claude CLI                  │
│  (实现任务，更新 TASKS.md)          │
└─────────────────────────────────────┘
```

### TASKS.md 格式

```markdown
# Project: 构建一个 TODO 应用

## Tasks
- [ ] Task 1: 实现用户认证 - 验收：可以注册和登录
- [ ] Task 2: 添加 CRUD 功能 - 验收：可以创建、读取、更新、删除 TODO
- [ ] Task 3: 编写单元测试 - 验收：测试覆盖率 > 80%
- [x] Task 4: 初始化项目结构 - 验收：已有基础目录结构

## Validation
npm test
npm run lint
```

**关键**：
- `- [ ]` 未完成，`- []` 已完成
- 全部 `- [x]` 时循环自动停止
- 每次迭代都加载此文件（保持简洁）

### loop.js（完整代码）

```javascript
#!/usr/bin/env node
const fs = require('fs');
const { execSync } = require('child_process');

let iteration = 0;
while (true) {
  iteration++;
  console.log(`\n=== Iteration ${iteration} ===\n`);

  const tasks = fs.readFileSync('TASKS.md', 'utf8');

  if (!tasks.includes('- [ ]')) {
    console.log('✅ All tasks complete!');
    break;
  }

  execSync('claude -p "Implement the next incomplete task in TASKS.md. Update the checkbox to [x] when done."', {
    stdio: 'inherit',
    shell: true
  });
}
```

**仅 10 行代码**，跨平台运行。

---

## 为什么这么简单？

### 第一性原理分析

我们问：**"Ralph Wiggum 最少需要什么才能工作？"**

**基本真理**：
1. **AI 需要多次尝试** → `while (true)` 循环
2. **状态必须持久化** → 单个文件存储
3. **必须检测完成** → 检查任务状态
4. **必须自动运行** → 无人干预执行

**除此之外的一切都是可选的。**

### 对比：重构前 vs 重构后

| 指标 | 重构前 | 重构后 | 减少 |
|------|--------|--------|------|
| 核心文件 | 7 | 4 | 43% |
| 状态文件 | 2 | 1 | 50% |
| 循环代码 | 66 行 | 10 行 | 85% |
| 设置复杂度 | 交互式 5+ 问题 | 编辑 1 个文件 | 复杂 → 简单 |
| 模式 | 2 (Build + Plan) | 1 | 50% |
| 核心概念 | 10+ | 3 | 70% |

### 移除了什么？

- ❌ **Plan 模式** - 规划 ≠ 迭代，分离关注点
- ❌ **双状态文件** - IMPLEMENTATION_PLAN.md + AGENTS.md → TASKS.md
- ❌ **平台特定脚本** - loop.sh + loop.bat → loop.js (Node.js)
- ❌ **PROMPT 模板** - 复杂性不解决核心问题
- ❌ **自动 Git 提交** - 可选增强，非核心
- ❌ **Marketplace 集成** - 文档化为可选

### 保留了什么？

- ✅ **Checkbox 格式** - 简单、有效
- ✅ **循环机制** - 基本真理 #1
- ✅ **任务持久化** - 基本真理 #2
- ✅ **完成检测** - 基本真理 #3
- ✅ **Claude CLI 调用** - 执行工作的 AI

---

## 使用示例

### 新项目

```bash
# 1. 创建项目
mkdir my-project && cd my-project
git init

# 2. 复制模板
cp /path/to/ralph-wiggum/TASKS.template.md TASKS.md

# 3. 编辑任务
vim TASKS.md

# 4. 复制循环脚本
cp /path/to/ralph-wiggum/loop.js .
chmod +x loop.js

# 5. 运行
./loop.js
```

### 现有项目

```bash
cd existing-project

# 创建 TASKS.md
cat > TASKS.md << EOF
# Project: 添加新功能

## Tasks
- [ ] 实现功能 A
- [ ] 编写测试
- [ ] 更新文档

## Validation
npm test
npm run lint
EOF

# 运行
./loop.js
```

---

## 可选增强

这些不是核心的一部分，但可以按需添加：

### Git 自动提交

在 `loop.js` 中，`execSync` 调用后添加：

```javascript
execSync('git add -A && git commit -m "iteration ' + iteration + '" && git push', { stdio: 'inherit' });
```

### Debug Skills

安装 marketplace：

```bash
claude plugin marketplace add liuxiaoyusky/ai-developer-skills
```

在 `TASKS.md` 中使用：

```markdown
## Tasks
- [ ] 修复 Bug - 卡住时使用 /debug "错误详情"
```

### 模型选择

通过环境变量：

```bash
CLAUDE_MODEL=opus ./loop.js
```

---

## 迁移指南（从旧版本）

### 如果你有 AGENTS.md

```bash
# 重命名
mv AGENTS.md TASKS.md

# 可选：删除非必要部分
# 仅保留 Tasks 和 Validation
```

### 如果你有 loop.sh 或 loop.bat

```bash
# 替换为 loop.js
# 无需其他更改
```

### 如果你使用 Plan 模式

```bash
# 旧工作流
/ralph-wiggum plan  # 交互式配置

# 新工作流
vim TASKS.md         # 直接编辑
./loop.js            # 运行
```

---

## 最佳实践

### 1. 任务拆分

**好的任务**：
- ✅ 实现用户登录功能（包含表单验证、JWT token）
- ✅ 添加数据持久化（使用 SQLite，CRUD 操作）

**不好的任务**：
- ❌ 实现完整应用（太大）
- ❌ 修复 bug（太模糊）

### 2. Let Ralph Ralph

- ✅ 信任 Ralph，让它自己决定如何实现
- ✅ 接受迭代，第一次可能不完美
- ✅ 观察和学习，注意 Ralph 如何解决问题

### 3. 保持 TASKS.md 简洁

- ✅ 定期删除已完成的任务
- ✅ 只包含必要信息
- ❌ 不要在文件中记录状态更新（用 Git commit）

---

## 常见问题

### Q: 循环不前进怎么办？

A: 检查以下几点：
1. TASKS.md 中的任务描述是否清晰
2. Validation 命令是否正确
3. 如果问题持续，手动修改 TASKS.md

### Q: 如何停止循环？

A: 按 `Ctrl+C` 停止循环。或者等待所有任务完成，循环会自动停止。

### Q: 支持哪些操作系统？

A: Linux、macOS 和 Windows。Node.js 跨平台运行。

### Q: 可以选择使用哪个模型吗？

A: 可以！通过环境变量：`CLAUDE_MODEL=opus ./loop.js`

### Q: 与官方插件的区别？

A: 本版本使用外部 Bash/Node.js 循环，更简单、更可控。官方版本使用 Stop Hook，更集成但较复杂。

---

## 文件结构

```
ralph-wiggum/
├── SKILL.md              # Skill 定义（本文档）
├── README.md             # 用户文档
├── TASKS.template.md     # TASKS.md 模板
├── loop.js               # 循环脚本（10 行）
└── LICENSE               # MIT License
```

---

## 参考资源

- [第一性原理思维](https://github.com/anthropics/claude-code/blob/main/docs/guides/first-principles.md)
- [Geoffrey Huntley 的原始实现](https://github.com/ghuntley/how-to-ralph-wiggum)
- [Claude Code 官方 Ralph Wiggum 插件](https://github.com/anthropics/claude-code/blob/main/plugins/ralph-wiggum/README.md)
- [Ralph Wiggum Guide - Awesome Claude AI](https://awesomeclaude.ai/ralph-wiggum)
- [A Brief History of Ralph](https://www.humanlayer.dev/blog/brief-history-of-ralph)

---

## 许可证

MIT License - 详见 [LICENSE](LICENSE)

---

**版本**: v2.0.0 (First-Principles Refactor)
**最后更新**: 2025-01-26

**极简即是强大！** 🚀
